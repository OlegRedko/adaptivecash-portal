# platform-core — candidate-owned package

The package is intentionally minimal. Define the portal module contract, tenant/user/permission context, shell/navigation composition and providers here. Avoid importing a concrete portal app.
